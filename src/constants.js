const BREAKPOINTS = {
  lg: 1300,
  md: 950,
  sm: 600,
}

export const QUERIES = {
  lg: `(max-width: ${BREAKPOINTS.lg / 16}rem)`,
  md: `(max-width: ${BREAKPOINTS.md / 16}rem)`,
  sm: `(max-width: ${BREAKPOINTS.sm / 16}rem)`,
}

